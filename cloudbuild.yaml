steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/transport-passenger-api'
      - './projfix'

images:
  - 'gcr.io/$PROJECT_ID/transport-passenger-api'

options:
  logging: CLOUD_LOGGING_ONLY
